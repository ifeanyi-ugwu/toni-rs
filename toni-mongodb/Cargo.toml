[package]
name = "toni-mongodb"
version = "0.1.0"
edition = "2024"
description = "MongoDB integration for the Toni framework"
license = "MIT"
repository = "https://github.com/monterxto/toni-rs"
homepage = "https://github.com/monterxto/toni-rs"

[dependencies]
toni = { path = "../toni" }
mongodb = { version = "3" }
async-trait = "0.1"
tracing = "0.1"
